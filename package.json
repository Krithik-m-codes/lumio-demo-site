{
  "name": "shubpy-demo-site",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone client website that embeds the Shubpy chat widget — used for the client demo.",
  "scripts": {
    "dev": "next dev --port 4000",
    "build": "next build",
    "start": "next start --port 4000"
  },
  "dependencies": {
    "next": "^16.0.10",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
